/**
 * 
 */
package io.javabrains;

/** @author ajayrabidas | Nov 30, 2019 **/

public class LambdaExpressionsSyntax {
/*
	private void perform() {
		System.out.println("Hello World");
	}

	above method in lamda expressioin:
		
	aBlockOfCode = () -> {
		System.out.println("Hellow World");
	};
*/
	/* Examples of Lambda Expresson
	 * 
	 * 1. greetingFunction = () -> System.out.println("Hello World");
	 * 
	 * 2. doubleNumberFunction = (int a) -> 2 * a;
	 * 
	 * 3. addFunction = (int a, int b) -> a + b;
	 * 
	 * 4. safeDivideFunction = (int a, int b) -> {
	 * 		if( b == 0 ) return 0;
	 * 		return a / b;
	 * }
	 * 
	 * 5. stringLengthCount = (String s ) -> s.length();
	 * 
	 * A single line lambda expression must not have curly braces {}
	 * 
	 * 
	 * 
	 * 
	 * 
	*/
}
